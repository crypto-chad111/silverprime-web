import { AdminDashboardClient } from "./AdminDashboardClient";

export const metadata = { title: "Admin Dashboard — Silver Prime" };

export default function AdminDashboardPage() {
  return <AdminDashboardClient />;
}
