import { AdminLoginClient } from "./AdminLoginClient";

export const metadata = { title: "Admin — Silver Prime Founders Club" };

export default function AdminPage() {
  return <AdminLoginClient />;
}
